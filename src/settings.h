#ifndef SETTINGS_H
#define SETTINGS_H

// Initialize settings: create MCS folder and load persisted brush size
void initSettings();

// Persist current brush size to MCS
void saveBrushSize();

#endif // SETTINGS_H
