#ifndef INPUT_H
#define INPUT_H

#include "config.h"

// Current selected particle
extern Particle selectedParticle;

// Handle input, returns true if should exit
bool handleInput();

#endif // INPUT_H
